<?php

( defined( 'SITE' ) || defined( 'WP_ADMIN' ) ) || exit;

/** Cart Directory */
define( 'SITE_CART_DIR', '/cart' );

/** Cart Path */
define( 'SITE_CART_PATH', SITE_THEME_HTML_PATH . SITE_CART_DIR );
