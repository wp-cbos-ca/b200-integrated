/*
 header js
 */

(function($){
*/#nav
*/#menu
*/#more-menu
	var navWidth = $('#nav').width();
	var menuWidth = $('#menu').width();
	//loop through list items, add up width
	//hide items that don't fit into available width
	
	//put these (or rather) unhide them in the "more-menu"
	//which is vertical, as opposed to horizontal.
	//if possible, hide/unhide on a one to one basis.
	
	
	})(jQuery);